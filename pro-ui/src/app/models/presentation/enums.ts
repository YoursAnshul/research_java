export enum UserActions {
    Lock = 'lock',
    Unlock = 'unlock',
    Activate = 'activate',
    Deactivate = 'deactivate'
}

export enum UserRole {
    Admin = 1,
    ProjectTeam = 2,
    Interviewer = 3,
    OutcomesIT = 4
}
